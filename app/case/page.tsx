import { CaseClient } from './CaseClient'

export const metadata = { title: 'IP授权案例库 - 新文创老贾聊IP', description: 'IP方 × 品牌方 × 工厂的授权合作案例库' }

export default function CasePage() {
  return <CaseClient />
}
