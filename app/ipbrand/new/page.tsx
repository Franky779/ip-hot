import { IpNewClient } from './IpNewClient'

export const metadata = {
  title: '新增 IP - IP品牌库',
  description: '向 IP品牌库新增一条 IP 档案',
}

export default function IpNewPage() {
  return <IpNewClient />
}
