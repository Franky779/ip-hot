# Copy this file to /srv/apps/rsshub/shared/rsshub.env on the server.
# Keep the real file owned by root with mode 0600. Never commit a real token.
TWITTER_AUTH_TOKEN=replace-with-a-dedicated-x-account-auth-token
