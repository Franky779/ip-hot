limit_req_zone $binary_remote_addr zone=ip_hot_api:10m rate=30r/s;

upstream ip_hot_next {
    server 127.0.0.1:3101;
    keepalive 32;
}

# hot.laojia-ip.com -> 301 to www.laojia-ip.com
server {
    listen 80;
    listen [::]:80;
    server_name hot.laojia-ip.com;
    return 301 https://www.laojia-ip.com$request_uri;
}

server {
    listen 443 ssl;
    listen [::]:443 ssl ipv6only=on;
    server_name hot.laojia-ip.com;

    ssl_certificate /etc/letsencrypt/live/hot.laojia-ip.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/hot.laojia-ip.com/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    return 301 https://www.laojia-ip.com$request_uri;
}

# HTTP -> HTTPS for main domain
server {
    listen 80;
    listen [::]:80;
    server_name laojia-ip.com www.laojia-ip.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    return 301 https://www.laojia-ip.com$request_uri;
}

# Main site
server {
    listen 443 ssl;
    listen [::]:443 ssl;
    http2 on;
    server_name laojia-ip.com www.laojia-ip.com;

    server_tokens off;
    client_max_body_size 50m;

    ssl_certificate /etc/letsencrypt/live/laojia-ip.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/laojia-ip.com/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 5;
    gzip_min_length 1024;
    gzip_types application/javascript application/json application/manifest+json application/xml font/woff2 image/svg+xml text/css text/javascript text/plain text/xml;

    add_header Strict-Transport-Security "max-age=31536000" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;

    location /research-assets/ {
        proxy_pass http://ip_hot_next;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        proxy_hide_header X-Powered-By;
        expires 30d;
        add_header Cache-Control "public, max-age=2592000, immutable" always;
        access_log off;
    }

    location /research-images/ {
        alias /srv/www/research-images/;
        expires 30d;
        add_header Cache-Control "public, max-age=2592000, immutable";
        access_log off;
    }

    # IP品牌库 images (2.64GB, gitignore excluded): served from shared dir
    location /ipbrand/images/ {
        alias /srv/apps/ip-hot/shared/ipbrand-images/;
        expires 30d;
        add_header Cache-Control "public, max-age=2592000, immutable";
        access_log off;
    }

    # IP品牌库 品牌手册图（PDF转图，仅展示不下载）
    location /ipbrand/manuals/ {
        alias /srv/apps/ip-hot/shared/ipbrand-manuals/;
        expires 30d;
        add_header Cache-Control "public, max-age=2592000, immutable";
        access_log off;
    }

    # IP工厂供应链库 images
    location /factory/images/ {
        alias /srv/apps/ip-hot/shared/factory-images/;
        expires 30d;
        add_header Cache-Control "public, max-age=2592000, immutable";
        access_log off;
    }

    location /_next/static/ {
        proxy_pass http://ip_hot_next;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        proxy_hide_header X-Powered-By;
        expires 1y;
        add_header Cache-Control "public, max-age=31536000, immutable" always;
        access_log off;
    }

    location /api/ {
        limit_req zone=ip_hot_api burst=60 nodelay;
        proxy_pass http://ip_hot_next;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        proxy_hide_header X-Powered-By;
        proxy_buffering off;
        proxy_read_timeout 330s;
        proxy_send_timeout 330s;
    }

    location / {
        proxy_pass http://ip_hot_next;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        proxy_hide_header X-Powered-By;
        proxy_buffering off;
        proxy_read_timeout 330s;
        proxy_send_timeout 330s;
    }
}